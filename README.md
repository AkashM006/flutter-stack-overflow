# stack_overflow

A Flutter project.

## Getting Started

### How to Run:

- Run this command to install packages: `flutter pub get`

- Before making any code changes to files related to database ( dao files, database file ) run the following command: `dart run build_runner watch` to make `build_runner` watch for code changes and generate files which drift uses.
